INSTRUÇÕES V0.3

O V0.3 foi feito um código simples no Arduino, e o JSON para importação do node-red está no drive.


Passo a passo:

Abrir o arquivo .ino e passar o código para o Arduino
Rodar o node-red no cmd e importar o flow pelo json
Abrir o XAMMP, rodar o Apache e o MYSQL, acessar o phpmyadmin, criar o db 'arduino_db' e a table 'dados sensores' onde vai ter duas colunas com os valores de tempo (valor: timestamp) e os valores dos sensores (dados, valor: INT).

Comandos importantes:

node-red
TRUNCATE TABLE * (APAGAR OS DADOS DE UMA TABELA SEM APAGAR A MESMA)
locahost/ui (Acessar o dashboard do node-red)